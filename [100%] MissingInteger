function solution(A) {
  // write your code in JavaScript (Node.js 8.9.4)
  let solution;
  let uniqueElObj=new Set([...A]);
  for(i=0;i<A.length;i++){
    if(!uniqueElObj.has(i+1)){
      return i+1
    }
    solution=i+2
  }
  return solution
}
