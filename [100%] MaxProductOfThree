function solution(A) {
  // write your code in JavaScript (Node.js 8.9.4)
  A.sort((a,b)=>{return b-a});
  let solution;
  if(A[0]*A[1]*A[2]>A[0]*A[A.length-1]*A[A.length-2]){
    solution=A[0]*A[1]*A[2]
  }else{
    solution=A[0]*A[A.length-1]*A[A.length-2]
  }
  return solution
}
